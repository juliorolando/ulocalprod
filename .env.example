# Copiar como .env y completar los valores

NODE_ENV=production
PORT=3000

# Admin
ADMIN_USER=admin
ADMIN_PASS=CAMBIAR_POR_CLAVE_SEGURA

# Sesión — generar con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET=CAMBIAR_POR_STRING_ALEATORIO_64_CHARS

# Base de datos
DB_PATH=./ushuaialocal.db
